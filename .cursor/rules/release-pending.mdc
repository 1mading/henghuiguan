---
description: 功能调整后累计待发变更，仅在用户确认发版后写入「更新记录」
alwaysApply: true
---

# 发版待确认 → 更新记录

用户可见的功能/修复完成后，按以下流程处理系统更新说明（不要自动发版）。

## 每次调整后（强制）

完成任何用户可见的功能/修复后，**必须**在同一次回复里做完下面两步，缺一不可：

1. 写入待发列表（二选一）：
   - `cd server && npm run record-change -- --type feature|fix|improve --text "用户可读的说明"`
   - 或直接编辑 [`server/releases/_pending.json`](server/releases/_pending.json) 的 `items`
2. **在回复末尾主动问一句**：「是否现在发版？」——即使只改了一处、也不要默认跳过

用户未明确说「发版 / 发布 / 确认发版」之前：
- 只累计到 `_pending.json`
- **不要**运行 `publish-release`
- **不要**把待发内容写进数据库或正式版本 json

## 用户确认发版后

在 `server` 目录执行：

```bash
npm run publish-release
# 或指定版本号 / 升级幅度：
# npm run publish-release -- --bump minor --title "标题" --summary "摘要"
# npm run publish-release -- 1.2.0 --title "标题"
```

发布后正式版本会写入 `server/releases/x.y.z.json` 并合并进 `systemUpdates`；全员可在「更新记录」查看，未读用户下次登录会弹窗。若当前服务进程未重启且未即时合并，可提示重启后端或再跑一次 publish。

## 约定

- `type`：`feature`（新功能）/ `fix`（修复）/ `improve`（优化）
- 文案面向业务用户，避免内部实现细节与密钥
- `_pending.json` 可提交 Git；`server/data/` 仍禁止提交
- 纯文档/注释/不影响用户的改动：可不记 pending，也无需询问发版
