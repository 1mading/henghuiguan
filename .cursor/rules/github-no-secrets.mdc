---
description: GitHub 上传安全 — 禁止提交密钥与业务敏感数据
alwaysApply: true
---

# GitHub 上传安全规则

协助用户推送到 GitHub 时，**必须**遵守以下规则。

## 禁止提交（永远不要 add/commit）

| 类型 | 路径 |
|------|------|
| 环境变量/密钥 | `server/.env`、`server/miniapp/config.js` |
| 业务数据库 | `server/data/`（含 `henghuiguan.json`、backups、uploads） |
| 真实 seed | `server/src/db/seed-data.json`、`seed-data.local.json` |
| 证书/密钥文件 | `*.pem`、`*.key`、`*.p12` |

## 允许提交的模板

- `server/.env.example`（占位符，无真实密钥）
- `server/miniapp/config.js.example`
- `server/src/db/seed-data.example.json`（演示人员，无真实 userid）

## AI 行为要求

1. **不要**把 `.env`、`config.js`、真实 `seed-data.json` 加入 commit
2. **不要**在代码/文档中写入 AppSecret、JWT_SECRET、API_KEY 的真实值
3. 用户要求 push 到 GitHub 前，先运行 `node scripts/check-secrets.js --staged`
4. 若 `恒慧管.html` 或 seed 含大量真实 `dingTalkUserId`（纯数字），提醒用户改用演示数据或 `git rm --cached`
5. 已从 Git 追踪的敏感文件，用 `git rm --cached <path>` 移除，**不要** `git push --force` 除非用户明确要求

## 首次启用 Git 钩子

```bash
git config core.hooksPath .githooks
```

## 本地真实数据

- 复制 `server/.env.example` → `server/.env` 并填写
- 复制 `server/miniapp/config.js.example` → `config.js`
- 复制 `seed-data.example.json` → `seed-data.local.json`（可选，含真实人员时用 local 文件）
